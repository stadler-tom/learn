services:
  web:
    image: learning-journal:latest
    ports:
      - "8080:8080"
    depends_on:
      - database

  database:
    image: postgres:14.24-alpine3.23
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: "db"
      POSTGRES_HOST_AUTH_METHOD: "trust"
